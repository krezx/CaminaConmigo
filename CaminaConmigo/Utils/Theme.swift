import SwiftUI

extension Color {
    static let customBackground = Color(UIColor.systemBackground)
    static let customSecondaryBackground = Color(UIColor.secondarySystemBackground)
    static let customGroupedBackground = Color(UIColor.systemGroupedBackground)
    static let customText = Color(UIColor.label)
    static let customSecondaryText = Color(UIColor.secondaryLabel)
    static let customTertiaryText = Color(UIColor.tertiaryLabel)
    static let customSeparator = Color(UIColor.separator)
    
    // Color principal de la app
    static let primaryApp = Color(UIColor(red: 239/255, green: 96/255, blue: 152/255, alpha: 1.0))
} 